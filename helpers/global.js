/**
 * Define here all helpers file as global variable
 */

// Provide sucess and error related response method 
if (!global.RESPONSE)
    global.RESPONSE = require('./response.js');


if (!global.FUNCTIONS)
    global.FUNCTIONS = require('./function.js');




